# Changelog

All notable changes to this project will be documented in this file.

## Unreleased

- Establish the Phase 1 data pipeline foundation.
- Add the UI-1 read-only React and FastAPI dashboard.
- Document the current implementation boundary, first real-data workflow, UI startup, and port troubleshooting.
- Add an adaptive mobile and tablet information architecture with fixed bottom navigation, card-based records, full-width drawers, safe-area support, and a touch-oriented annotation workspace.
- Add reviewed mobile screenshots and bilingual documentation without publishing customer tasks, business media, deployment addresses, or runtime metrics.
- Harden deployment package verification so root runtime directories are excluded without removing required Python packages such as `yolo_factory.registry` and `yolo_factory.models`.
